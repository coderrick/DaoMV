const ScheDapp = artifacts.require('ScheDapp');

describe("Supply Eth to Compound", () => {
    it("should success", async () => {
        // const scheDapp = ScheDapp.deployed();

        // scheDapp.supplyEthToCompound();
    });
});